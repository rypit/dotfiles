#!/bin/bash

DIR="$(cd "$(dirname "$0")/../" && pwd)"
RESULT=$(shellcheck -e SC1090 "$DIR"/*/* .bash_profile)

if [ "$RESULT" == "" ] ; then
  echo "✅ Shellecheck passed"
else
  echo "❌ Shellcheck failed"
  echo "$RESULT"
  exit 1;
fi
