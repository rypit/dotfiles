#!/bin/bash
DOTFILES_BASE="$(cd "$(dirname "$0")/../" && pwd)"
rm -f ~/.bash_profile
ln -sf "$DOTFILES_BASE"/.bash_profile ~/.bash_profile
source ~/.bash_profile

# Configure git
ln -sf "$DOTFILES_BASE"/.gitignore_global ~/.gitignore_global
git config --global core.excludesfile ~/.gitignore_global
